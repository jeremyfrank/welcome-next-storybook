import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { Card } from '../components/Card.tsx';
import { Button } from './Button'

<Meta link="/foo" component={Card} />

export const Template = (args) => <Card {...args} />;

# Card

Some intro text for this story.

<Canvas>
  <Story
    name="Default Card"
    args={{ 
      link: '/my-page',
      featured: false,
      children: <Button size="small" label="Button" />,
    }}
  >
    {(args) => <Card {...args} />}
  </Story>
  
  <Story
    name="Featured Card"
    args={{
      link:'/my-page',
      featured: true,
      children: <Button size="small" label="Featured Button" />
    }}
  >
    {(args) => <Card {...args} />}
  </Story>
</Canvas>